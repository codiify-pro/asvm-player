name: Build ASVM Player

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install
        run: npm install

      - name: Build App
        run: npm run build

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: ASVM-Player
          path: dist/*
